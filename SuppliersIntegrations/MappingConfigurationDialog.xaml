<Window x:Class="BOMVIEW.MappingConfigurationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BOM Template Configuration" 
        Height="790" 
        Width="710"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F7FA">
    <Window.Resources>
        <!-- Colors -->
        <Color x:Key="PrimaryColor">#3B82F6</Color>
        <Color x:Key="SecondaryColor">#6366F1</Color>
        <Color x:Key="AccentColor">#10B981</Color>
        <Color x:Key="WarningColor">#F59E0B</Color>
        <Color x:Key="ErrorColor">#EF4444</Color>
        <Color x:Key="LightGrayColor">#F3F4F6</Color>
        <Color x:Key="DarkGrayColor">#4B5563</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
        <SolidColorBrush x:Key="LightGrayBrush" Color="{StaticResource LightGrayColor}"/>
        <SolidColorBrush x:Key="DarkGrayBrush" Color="{StaticResource DarkGrayColor}"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#FFFFFF"/>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2563EB"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Simplified Modern ComboBox Style -->
        <Style x:Key="SimplifiedComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="toggleButton"
                                  BorderBrush="{TemplateBinding BorderBrush}"
                                  BorderThickness="{TemplateBinding BorderThickness}"
                                  Background="{TemplateBinding Background}"
                                  Padding="{TemplateBinding Padding}"
                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateRoot"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"
                                                          HorizontalAlignment="Left"
                                                          VerticalAlignment="Center"
                                                          Margin="{TemplateBinding Padding}"
                                                          Content="{Binding SelectedItem.Name, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"/>
                                                <Path x:Name="arrow"
                                              Grid.Column="1"
                                              Width="10"
                                              Height="6"
                                              Margin="0,0,12,0"
                                              Fill="#6B7280"
                                              Data="M 0,0 L 10,0 L 5,6 Z"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="BorderBrush" Value="#3B82F6"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter Property="BorderBrush" Value="#3B82F6"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup"
                           IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                           AllowsTransparency="True"
                           Placement="Bottom"
                           PlacementTarget="{Binding ElementName=toggleButton}">
                                <Border x:Name="dropDownBorder"
                                Background="White"
                                BorderBrush="#E5E7EB"
                                BorderThickness="1"
                                CornerRadius="8"
                                MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                MinWidth="{Binding ActualWidth, ElementName=toggleButton}">
                                    <ScrollViewer CanContentScroll="True">
                                        <ItemsPresenter Margin="4"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" 
                           Padding="8,6" 
                           FontSize="13"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Padding" Value="4"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="itemBorder"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4"
                                    Background="Transparent">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" TargetName="itemBorder" Value="#F3F4F6"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" TargetName="itemBorder" Value="#EBF5FF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F3F4F6"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#E5E7EB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Warning Button Style -->
        <Style x:Key="WarningButton" TargetType="Button">
            <Setter Property="Background" Value="#FEF2F2"/>
            <Setter Property="Foreground" Value="#EF4444"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#FCA5A5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FEE2E2"/>
                                <Setter Property="BorderBrush" Value="#EF4444"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FEE2E2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Text Box Style - UPDATED -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Padding" Value="12,4"/>
            <!-- Reduced vertical padding -->
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                Margin="{TemplateBinding Padding}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern ComboBox Style -->
        <!-- Modern ComboBox Style - Optimized for Template Display -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <!-- Override for how items are displayed in the dropdown list -->
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" Padding="4,6" FontSize="13"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                              Background="{TemplateBinding Background}"
                              BorderBrush="{TemplateBinding BorderBrush}"
                              BorderThickness="{TemplateBinding BorderThickness}"
                              IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                              Focusable="false">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="30" />
                                                </Grid.ColumnDefinitions>
                                                <!-- Content presenter for the selected item -->
                                                <ContentControl x:Name="ContentPresenter"
                                                  Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left">
                                                    <TextBlock Text="{Binding SelectedItem.Name, RelativeSource={RelativeSource AncestorType=ComboBox}}" 
                                                       VerticalAlignment="Center"/>
                                                </ContentControl>
                                                <!-- Arrow indicator -->
                                                <Path x:Name="Arrow"
                                              Data="M7 10l5 5 5-5z"
                                              Fill="#6B7280"
                                              Grid.Column="1"
                                              Width="16"
                                              Height="16"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter Property="Background" Value="#F9FAFB" />
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="true">
                                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                           Placement="Bottom"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           AllowsTransparency="True"
                           Focusable="False"
                           PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                              SnapsToDevicePixels="True"
                              MinWidth="{TemplateBinding ActualWidth}"
                              MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                    Background="White"
                                    BorderBrush="#E5E7EB"
                                    BorderThickness="1"
                                    CornerRadius="8">
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <ItemsPresenter />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern CheckBox Style -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,8,0,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid x:Name="templateRoot" Background="Transparent" SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="checkBoxBorder" 
                                    Background="White"
                                    BorderBrush="#E5E7EB" 
                                    BorderThickness="1"
                                    HorizontalAlignment="Left" 
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center" 
                                    Width="18" 
                                    Height="18" 
                                    CornerRadius="4">
                                <Path x:Name="checkMark" 
                                      Data="M4 10l3 3 6-6" 
                                      Stroke="{StaticResource PrimaryBrush}" 
                                      StrokeThickness="2"
                                      Visibility="Collapsed"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                            </Border>
                            <ContentPresenter x:Name="contentPresenter" 
                                              Grid.Column="1" 
                                              Focusable="False" 
                                              HorizontalAlignment="Left" 
                                              Margin="{TemplateBinding Padding}" 
                                              RecognizesAccessKey="True" 
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                              VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="true">
                                <Setter Property="Visibility" TargetName="checkMark" Value="Visible"/>
                                <Setter Property="Background" TargetName="checkBoxBorder" Value="#EBF5FF"/>
                                <Setter Property="BorderBrush" TargetName="checkBoxBorder" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="checkBoxBorder" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern DataGrid Style -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E5E7EB"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="#F9FAFB"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="RowHeight" Value="44"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>

        <!-- DataGrid Column Header Style -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,14"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="SeparatorBrush" Value="Transparent"/>
        </Style>

        <!-- DataGrid Cell Style -->
        <!-- DataGrid Cell Style -->
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,4"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#2563EB"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Icons -->
        <PathGeometry x:Key="IconSave">M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z M17 21v-8H7v8 M7 3v5h8</PathGeometry>
        <PathGeometry x:Key="IconDetect">M10 18a8 8 0 1 0 0-16 8 8 0 0 0 0 16zM21 21l-4.35-4.35</PathGeometry>
        <PathGeometry x:Key="IconEdit">M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7 M18.5 2.5a2.12 2.12 0 0 1 3 3L12 15l-4 1 1-4Z</PathGeometry>
        <PathGeometry x:Key="IconTrash">M3 6h18 M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6 M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2 M10 11v6 M14 11v6</PathGeometry>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" 
                Background="White" 
                CornerRadius="16"
                Padding="20,16"
                Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect Color="#000000" 
                                  BlurRadius="16" 
                                  ShadowDepth="2" 
                                  Opacity="0.08"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Icon -->
                <Border Grid.Column="0" 
Width="28" Height="28" 
Background="#217346" 
CornerRadius="7"
Margin="0,0,10,0" 
VerticalAlignment="Center">
                    <!-- Simple Excel "X" symbol -->
                    <TextBlock Text="X"
       FontWeight="Bold"
       FontSize="16"
       Foreground="White"
       HorizontalAlignment="Center"
       VerticalAlignment="Center"/>
                </Border>

                <!-- Header Text -->
                <TextBlock Grid.Column="1"
                           x:Name="HeaderText"
                           Text="BOM Template Configuration"
                           FontSize="20"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Foreground="#111827"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Template Management -->
            <!-- Left Panel - Template Management -->
            <Border Background="White" 
        CornerRadius="16"
        Padding="20"
        Margin="0,0,16,0">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" 
                          BlurRadius="16" 
                          ShadowDepth="2" 
                          Opacity="0.08"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Template Management" 
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="#111827"
                   Margin="0,0,0,16"/>

                    <TextBlock Text="Select Template" 
           FontSize="14"
           FontWeight="Medium"
           Foreground="#4B5563"
           Margin="0,0,0,8"/>

                    <ComboBox x:Name="TemplateSelector" 
          Style="{StaticResource SimplifiedComboBox}"
          DisplayMemberPath="Name"
          Margin="0,0,0,16"
          SelectionChanged="TemplateSelector_SelectionChanged"/>



                    <!-- Template Action Buttons -->
                    <UniformGrid Columns="2" Rows="2" Margin="0,0,0,24">
                        <Button x:Name="btnRename"
                    Style="{StaticResource SecondaryButton}"
                    Click="btnRename_Click"
                    Margin="0,0,4,4"
                    Height="38">
                            <StackPanel Orientation="Horizontal">
                                <Border Background="#F3F4F6" 
                            Width="26" 
                            Height="26" 
                            CornerRadius="6">
                                    <Path Data="{StaticResource IconEdit}"
                              Width="14" Height="14"
                              Stroke="#4B5563"
                              StrokeThickness="1.5"
                              Stretch="Uniform"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Rename"
                               FontWeight="SemiBold"
                               FontSize="13"
                               Foreground="#111827"
                               Margin="8,0,0,0"
                               VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="btnDelete"
                    Style="{StaticResource SecondaryButton}"
                    Click="btnDelete_Click"
                    Margin="4,0,0,4"
                    Height="38">
                            <StackPanel Orientation="Horizontal">
                                <Border Background="#FEF2F2" 
                            Width="26" 
                            Height="26" 
                            CornerRadius="6">
                                    <Path Data="{StaticResource IconTrash}"
                              Width="14" Height="14"
                              Stroke="#EF4444"
                              StrokeThickness="1.5"
                              Stretch="Uniform"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Delete"
                               FontWeight="SemiBold"
                               FontSize="13"
                               Foreground="#111827"
                               Margin="8,0,0,0"
                               VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="btnUpdate"
        Style="{StaticResource SecondaryButton}"
        Click="btnUpdate_Click"
        Margin="0,4,4,0"
        Height="38">
                            <StackPanel Orientation="Horizontal">
                                <Border Background="#ECFDF5" 
                Width="26" 
                Height="26" 
                CornerRadius="6">
                                    <!-- Using a pre-defined icon path that clearly shows the refresh symbol -->
                                    <Path Data="M17.65,6.35C16.2,4.9 14.21,4 12,4A8,8 0 0,0 4,12A8,8 0 0,0 12,20C15.73,20 18.84,17.45 19.73,14H17.65C16.83,16.33 14.61,18 12,18A6,6 0 0,1 6,12A6,6 0 0,1 12,6C13.66,6 15.14,6.69 16.22,7.78L13,11H20V4L17.65,6.35Z"
                  Width="14" Height="14"
                  Fill="#10B981"
                  Stretch="Uniform"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Update"
                   FontWeight="SemiBold"
                   FontSize="13"
                   Foreground="#111827"
                   Margin="8,0,0,0"
                   VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button x:Name="btnSaveAs"
                    Style="{StaticResource SecondaryButton}"
                    Click="btnSaveAs_Click"
                    Margin="4,4,0,0"
                    Height="38">
                            <StackPanel Orientation="Horizontal">
                                <Border Background="#EFF6FF" 
                            Width="26" 
                            Height="26" 
                            CornerRadius="6">
                                    <Path Data="{StaticResource IconSave}"
                              Width="14" Height="14"
                              Stroke="#3B82F6"
                              StrokeThickness="1.5"
                              Stretch="Uniform"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="Save As"
                               FontWeight="SemiBold"
                               FontSize="13"
                               Foreground="#111827"
                               Margin="8,0,0,0"
                               VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </UniformGrid>

                    <!-- Configuration Section -->
                    <TextBlock Text="Configuration" 
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="#111827"
                   Margin="0,0,0,16"/>

                    <TextBlock Text="Start Row" 
                   FontSize="14"
                   FontWeight="Medium"
                   Foreground="#4B5563"
                   Margin="0,0,0,8"/>

                    <TextBox x:Name="StartRowInput" 
                 Style="{StaticResource ModernTextBox}"
                 Margin="0,0,0,16"
                 PreviewTextInput="NumberValidationTextBox"/>

                    <TextBlock Text="Assembly Quantity" 
                   FontSize="14"
                   FontWeight="Medium"
                   Foreground="#4B5563"
                   Margin="0,0,0,8"/>

                    <TextBox x:Name="AssemblyQuantityInput" 
                 Style="{StaticResource ModernTextBox}"
                 Margin="0,0,0,16"
                 PreviewTextInput="NumberValidationTextBox"/>

                    <CheckBox x:Name="UseBufferCheckbox" 
                  Style="{StaticResource ModernCheckBox}"
                  Content="Add 10% quantity buffer (round up)" 
                  Margin="0,0,0,0"/>
                </StackPanel>
            </Border>


            <!-- Right Panel - Column Mapping -->
            <Border Grid.Column="1" 
        Background="White" 
        CornerRadius="16"
        Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" 
                          BlurRadius="16" 
                          ShadowDepth="2" 
                          Opacity="0.08"/>
                </Border.Effect>
                <DockPanel>
                    <TextBlock Text="Column Mapping" 
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="#111827"
                   Margin="0,0,0,8"
                   DockPanel.Dock="Top"/>

                    <TextBlock Text="Map your Excel columns to BOM fields" 
                   Foreground="#6B7280"
                   FontSize="14"
                   Margin="0,0,0,16"
                   DockPanel.Dock="Top"/>
                    <!-- Integrated Header Row and Auto-Detect Controls -->
                    <Border DockPanel.Dock="Top" 
        Background="#F9FAFB" 
        CornerRadius="8" 
        Padding="12" 
        Margin="0,0,0,16">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                   Text="Auto Detection" 
                   FontSize="14"
                   FontWeight="SemiBold"
                   Foreground="#111827"
                   Margin="0,0,0,8"/>

                            <!-- Integrated input-button combination -->
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" 
                    Background="White"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    CornerRadius="8,0,0,8"
                    Height="40">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                               Text="Header Row:" 
                               FontSize="14"
                               Foreground="#4B5563"
                               VerticalAlignment="Center"
                               Margin="12,0,8,0"/>

                                        <TextBox Grid.Column="1"
                             x:Name="HeaderRowInput" 
                             Text="1"
                             BorderThickness="0"
                             Background="Transparent"
                             VerticalContentAlignment="Center"
                             PreviewTextInput="NumberValidationTextBox"/>
                                    </Grid>
                                </Border>

                                <Button Grid.Column="1"
                    x:Name="btnAutoDetect"
                    Foreground="White"
                    Click="btnAutoDetect_Click"
                    Height="40"
                    BorderThickness="0"
                    Padding="16,0">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" 
                                CornerRadius="0,8,8,0"
                                Padding="{TemplateBinding Padding}">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                        <GradientStop Color="#3B82F6" Offset="0.0"/>
                                                        <GradientStop Color="#2563EB" Offset="1.0"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <!-- Add subtle inner shadow -->
                                                <Border.Effect>
                                                    <DropShadowEffect ShadowDepth="0" 
                                                Direction="270" 
                                                Color="#000000" 
                                                Opacity="0.2" 
                                                BlurRadius="4"/>
                                                </Border.Effect>
                                                <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" TargetName="border">
                                                        <Setter.Value>
                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                                <GradientStop Color="#2563EB" Offset="0.0"/>
                                                                <GradientStop Color="#1D4ED8" Offset="1.0"/>
                                                            </LinearGradientBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" TargetName="border">
                                                        <Setter.Value>
                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                                <GradientStop Color="#1D4ED8" Offset="0.0"/>
                                                                <GradientStop Color="#1E40AF" Offset="1.0"/>
                                                            </LinearGradientBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Setter Property="RenderTransform" TargetName="border">
                                                        <Setter.Value>
                                                            <TranslateTransform Y="1"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value="0.6"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <StackPanel Orientation="Horizontal">
                                        <!-- Enhanced icon with glow effect -->
                                        <Grid>
                                            <Ellipse Width="22" Height="22" 
                                 Fill="#FFFFFF" 
                                 Opacity="0.15"/>
                                            <Path Data="{StaticResource IconDetect}"
                              Width="16" Height="16"
                              Stroke="White"
                              StrokeThickness="1.5"
                              Stretch="Uniform"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                                        </Grid>
                                        <TextBlock Text="Auto-Detect"
                               FontWeight="SemiBold"
                               FontSize="13"
                               Foreground="White"
                               Margin="8,0,0,0"
                               VerticalAlignment="Center">
                        <!-- Text shadow for better readability -->
                                            <TextBlock.Effect>
                                                <DropShadowEffect ShadowDepth="1" 
                                              Direction="330" 
                                              Color="#000000" 
                                              Opacity="0.3" 
                                              BlurRadius="2"/>
                                            </TextBlock.Effect>
                                        </TextBlock>
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </Grid>
                    </Border>

                    <DataGrid x:Name="MappingGrid" 
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Margin="0,0,0,0"
                  CellEditEnding="MappingGrid_CellEditEnding">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="BOM Field" 
                                    Binding="{Binding Field}" 
                                    IsReadOnly="True"
                                    Width="150"/>
                            <DataGridComboBoxColumn x:Name="ColumnLetterColumn" 
                                        Header="Excel Column" 
                                        Width="100"
                                        SelectedValueBinding="{Binding SelectedColumn}"/>
                            <DataGridCheckBoxColumn Header="Required" 
                                        Binding="{Binding IsRequired}"
                                        Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="16"
                Padding="20,16"
                Margin="0,20,0,0">
            <Border.Effect>
                <DropShadowEffect Color="#000000" 
                                  BlurRadius="16" 
                                  ShadowDepth="2" 
                                  Opacity="0.08"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Right">
                <Button Style="{StaticResource SecondaryButton}"
                        Click="CancelButton_Click"
                        Margin="0,0,10,0"
                        Height="38">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Cancel"
                                   FontWeight="SemiBold"
                                   FontSize="13"
                                   Foreground="#111827"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource SecondaryButton}"
                        Click="ApplyButton_Click"
                        Height="38">
                    <StackPanel Orientation="Horizontal">
                        <Border Background="#ECFDF5" 
                                Width="26" 
                                Height="26" 
                                CornerRadius="6">
                            <Path Data="M5 13l4 4L19 7"
                                  Width="16" Height="16"
                                  Stroke="#10B981"
                                  StrokeThickness="1.5"
                                  Stretch="Uniform"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Apply"
                                   FontWeight="SemiBold"
                                   FontSize="13"
                                   Foreground="#111827"
                                   Margin="8,0,0,0"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
            <!-- Closes the StackPanel with the buttons -->
        </Border>
        <!-- Closes the footer Border -->
    </Grid>
    <!-- Closes the main Grid -->
</Window>
<!-- Closes the Window -->